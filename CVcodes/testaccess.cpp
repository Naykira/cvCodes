/*
	for(i = 0;i < rows;i++)
	{
		for(j = 0;j < cols;j++)
		{
			b = *img.data;
			img.data++;
			g = *img.data;
			img.data++;
			r = *img.data;
			img.data++;
			printf("%d %d - %d %d %d \n", i, j, b, g, r);
		}
	}
	*/